import React from "react";
import { Typography } from "@mui/material";

const JobTypePieChart = ({ data }) => (
  <div>
    <Typography variant="h6">Job Type Distribution (Placeholder)</Typography>
  </div>
);

export default JobTypePieChart;
