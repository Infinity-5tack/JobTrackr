import React from "react";
import { Typography } from "@mui/material";

const TopJobTitles = ({ data }) => (
  <div>
    <Typography variant="h6">Top Job Titles (Placeholder)</Typography>
  </div>
);

export default TopJobTitles;
